# Current Failed Unit Tests (Baseline)

The following unit tests are failing in the baseline (main branch) before any
changes or merges:

1. **packages/cli/src/ui/components/shared/text-buffer.test.ts**
   1. **Error:** Test timed out in 5000ms.
   2. **Test Case:**
      `useTextBuffer > Input Sanitization > should sanitize large ANSI text (>5000 chars) and strip escape codes`
